{ 
	This program is lexical and parsing error-free.
}

program errorfree;
type
	r = record
		a,b : integer;
		c,d   : string
	end;
        k = record
              mm : string;
              zz : integer 
        end;
	y = array[1..10] of integer;
	s = string;
        m = boolean;
 
var
	z : s;
        z1 : m;
        z21 : s;
        z4 : m;
        z : integer;
        de : m;
	
 
function foo1(a : integer; a : m; c : boolean; z2 : m; k : s) : integer;
var
        de : s;
begin
   a := de;
   a := z2;
   k := z4;
  z21 := a;
  z21 := c;
  z21 := z4;
  z21 := k; 
  z := 5;
  foo1 := 1;
end;

function foo2(a : integer; k : string) : y; 
begin

       k := foo1(-z* not z1 - z21*z4 + z1*z21 - z*de);	
       z21 := foo1(not z* not k1 or z);  	
       z := -z* not z1 - z21*z4 + z1*z21 - z*de;
       bar(z21*mm,c*d,a*b,-z4* not z4);
       boo(a*mm);
       z1:= z*z < z*y;
       m1:= m*y;
       z:= z11111*g;
       for x := 1 to 10 do
       begin
        s:=1;
        z := -z* not z1 - z21*z4 + z1*z21 - z*de;
	for y := 100 to 200 do
	begin
	  z:=1;
		for z := 10 to 20 do
		begin
		  k := 1;
		end;
	end;
	a := b * c[kk] +  d.e[f]*l - d*k;
       end;
       while x do
	s :=1;

       if y then  
	 k:=1
       else 
	 m:=1 		
	
end;

procedure foo12(a : integer; a : m; c : boolean; z2 : m; k : s); forward;


function foo3(a, b : integer) : string; forward;

begin
   z := -z* not z1 - z21*z4 + z1*z21 - z*de;
   k := not z* not k1 or z;
   x[k*y] := m.x;
   a := b * c[kk] +  d.e[f]*l - d*k;
   k := b*m + z* not z1;
end.

